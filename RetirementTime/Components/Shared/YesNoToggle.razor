@* Reusable pill toggle switch *@
<button type="button"
        class="ne-pill-toggle @(Value ? "ne-pill-toggle--on" : "")"
        @onclick="Toggle"
        aria-checked="@Value.ToString().ToLower()"
        role="switch">
    <span class="ne-pill-toggle__thumb"></span>
</button>

