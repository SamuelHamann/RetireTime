@page "/scenario/{scenarioId:long}/income/timeline/{timelineId:long}/defined-contribution"
@layout DashboardLayout
@rendermode InteractiveServer

@using RetirementTime.Models.Income

<div class="intro-step">
    <div class="step-header-animate">
        <h1 class="font-headline text-5xl font-bold text-ne-primary mt-3 mb-2">@Localizer["Income_DefinedContribution_Title"]</h1>
        <p class="font-body text-ne-on-surface-variant text-base leading-relaxed">@Localizer["Income_DefinedContribution_Subtitle"]</p>
    </div>
    <div class="step-body-animate">
        <div class="bg-ne-surface-low rounded-xl px-8 pb-8 pt-5 shadow-sm">

            <div class="income-section-header">
                <h2 class="assets-section-title">
                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/>
                    </svg>
                    @Localizer["Income_DefinedContribution_SectionTitle"]
                </h2>
            </div>

            <p class="field-sublabel" style="margin-bottom: 1.25rem;">@Localizer["Income_DefinedContribution_Hint"]</p>

            @if (_isLoading)
            {
                <div class="intro-loading">
                    <p>Loading...</p>
                </div>
            }
            else
            {
                <div class="income-items-list">
                    @foreach (var item in _planItems)
                    {
                        var index = _planItems.IndexOf(item);

                        <div class="income-item-card">

                            <div class="income-item-header">
                                <span class="income-item-number">@string.Format(Localizer["Income_DefinedContribution_Item"].Value, index + 1)</span>
                                <button type="button" class="income-remove-btn" @onclick="async () => await RemovePlan(item)">
                                    @Localizer["Income_Remove"]
                                </button>
                            </div>

                            <div class="income-fields-grid">

                                <div class="field-group income-field-full">
                                    <label class="field-question">@Localizer["Income_DefinedContribution_Name"]</label>
                                    <input type="text" class="field-input" @bind="item.Name" @bind:after="async () => await SavePlan(item)" />
                                </div>

                                <div class="field-group">
                                    <label class="field-question">@Localizer["Income_DefinedContribution_PercentEmployee"]</label>
                                    <div class="percent-input-wrapper">
                                        <input type="number" class="field-input percent-input" @bind="item.PercentOfSalaryEmployee" @bind:after="async () => await SavePlan(item)" min="0" max="100" step="0.01" />
                                    </div>
                                </div>

                                <div class="field-group">
                                    <label class="field-question">@Localizer["Income_DefinedContribution_PercentEmployer"]</label>
                                    <div class="percent-input-wrapper">
                                        <input type="number" class="field-input percent-input" @bind="item.PercentOfSalaryEmployer" @bind:after="async () => await SavePlan(item)" min="0" max="100" step="0.01" />
                                    </div>
                                </div>

                            </div>
                        </div>
                    }
                </div>
            }

            <button type="button" class="income-add-main-btn" @onclick="async () => await AddPlan()">
                + @Localizer["Income_DefinedContribution_AddPlan"]
            </button>

        </div>
    </div>
    <div class="income-next-row">
        <button class="income-next-btn" @onclick="NavigateNext">
            <span>@Localizer["Income_Next"]</span>
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                <line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/>
            </svg>
        </button>
    </div>
</div>

